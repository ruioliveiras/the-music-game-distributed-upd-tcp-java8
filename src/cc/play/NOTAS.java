/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package cc.play;

/**
 *
 * @author ruioliveiras
 */
public class NOTAS {
    /**
     * 
     * Modify the Pattern for this filosophy:
     *      "Each feature as one and only one Class" 
     * 
     * For that, in the case of Client and Server that are different entities we 
     * will use interfaces.
     * If an interface is not sufficient, just split into two classes, there isn't too.
     * 
     * When an interface is not sufficient, the application should be separated
     * into two project's: 
     *  server project that know everything
     *  client
     * 
     * if required create 3º project that is shared between this two.
     *  
     * 
     * 
     * 
     * 
     */
}
